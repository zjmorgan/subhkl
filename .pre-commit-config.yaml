repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.15.1 # Use the latest Ruff version
  hooks:
    # Run the linter (with auto-fix enabled)
    - id: ruff-check
      args: [ --fix ]
    # Run the formatter
    - id: ruff-format
